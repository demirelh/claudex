# =============================================================================
# Secrets — TEMPLATE ONLY
# =============================================================================
# In production, use sealed-secrets, external-secrets-operator, or Vault.
# This file is a reference. Do NOT commit real values.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: claudex-secrets
  namespace: claudex
type: Opaque
stringData:
  # Replace with real values via sealed-secrets or external-secrets
  GITHUB_MODELS_PAT: "github_pat_REPLACE_ME"
  LITELLM_MASTER_KEY: "sk-litellm-REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
  DATABASE_URL: "postgresql://litellm:REPLACE_ME@postgres:5432/litellm"
---
apiVersion: v1
kind: Secret
metadata:
  name: github-mcp-secret
  namespace: claudex
type: Opaque
stringData:
  GITHUB_PERSONAL_ACCESS_TOKEN: "github_pat_REPLACE_ME"
