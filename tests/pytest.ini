[pytest]
markers =
    integration: marks tests that require a running gateway (deselect with '-m "not integration"')
    guardrails: marks tests that require Presidio (deselect with '-m "not guardrails"')
testpaths = .
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
